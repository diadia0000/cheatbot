networks:
  cheet_net:
    driver: bridge

services:
  vllm_server:
    image: vllm/vllm-openai:latest-cu130
    container_name: vllm_server
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./models:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    environment:
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:/usr/local/nvidia/lib64:/usr/local/cuda/lib64
    command: >
      --model /models/Qwen2.5-14B-Instruct-AWQ --quantization awq --max-model-len 32768 --gpu-memory-utilization 0.7
    shm_size: '16gb'
    networks:
      - cheet_net

  fastapi_backend:
    build:
      context: ./backend
    container_name: fastapi_backend
    volumes:
      - ./backend:/app
      - ./data:/data
    environment:
      - VLLM_API_URL=http://vllm_server:8000/v1
      - MODEL_NAME=/models/Qwen2.5-14B-Instruct-AWQ
      - FAL_KEY=${FAL_KEY}
      - LINE_CHANNEL_SECRET=${LINE_CHANNEL_SECRET}
      - LINE_CHANNEL_ACCESS_TOKEN=${LINE_CHANNEL_ACCESS_TOKEN}
      - LINE_BACKEND_PUBLIC_URL=${LINE_BACKEND_PUBLIC_URL}
    depends_on:
      - vllm_server
    restart: on-failure
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - cheet_net

  webhook_gateway:
    image: nginx:alpine
    container_name: webhook_gateway
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./ops/nginx/webhook-only.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      fastapi_backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1/healthz"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    networks:
      - cheet_net

  streamlit_frontend:
    build:
      context: ./frontend
    container_name: streamlit_frontend
    ports:
      - "127.0.0.1:8501:8501"
    volumes:
      - ./frontend:/app
    environment:
      - BACKEND_API_URL=http://fastapi_backend:8080
    depends_on:
      fastapi_backend:
        condition: service_healthy
    restart: on-failure
    profiles:
      - ui
    networks:
      - cheet_net
