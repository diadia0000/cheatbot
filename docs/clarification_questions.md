# MVP 需求釐清問題

## 1. 圖片生成方式：Fal.ai 還是 Pillow 文字疊加？

SDD 寫的是用 Fal.ai image-to-image 結合底圖重繪，但你附的兩張截圖看起來是：
- **第二張（乾淨的股市隱者 APP 畫面）** = `basic-picture.png` 底圖
- **第一張（左上角有紅色文字 `--- 2026-03-03 操作績效 ---` + 多檔股票損益）** = 期望的輸出結果

如果目標是在底圖上精準疊上紅色中文文字和數字，**Fal.ai image-to-image 做不到這件事**（它會把整張圖重繪，中文字也渲染不準）。這應該用 **Pillow (PIL) 直接畫文字上去**才對。

**請確認：最終出圖方式是要用 Pillow 文字疊加，還是堅持用 Fal.ai？**

回答：


## 2. 圖片上要呈現的資訊格式

從第一張截圖來看，紅色文字的格式是：
```
--- 2026-03-03 操作績效 ---
標的：光聖 | 損益：+16.01%
標的：雷虎 | 損益：-8.41%
標的：百一 | 損益：+17.25%
標的：華通 | 損益：+14.54%
標的：燿華 | 損益：+16.23%
標的：明泰 | 損益：+14.50%
```

幾個子問題：
- (a) 這些股票是從 `get_top_stocks()` 撈出的漲幅最高的股票嗎？
- (b) 裡面故意混一檔虧損的（雷虎 -8.41%）是為了增加可信度嗎？這個邏輯要保留？
- (c) 損益數字要用 yfinance 抓到的**真實漲幅**，還是要**誇大**（如 SDD 寫的「把漲幅誇大，符合詐騙集團人設」）？
- (d) 要顯示幾檔股票？固定 6 檔還是隨機 3~6 檔？

回答：


## 3. 股票觀察池的具體清單

SDD 寫「鎖定台灣最有價值（權值股/0050成分股）的數十檔指標股票」，但截圖裡出現的是光聖、雷虎、百一、華通這類**中小型股**，不是 0050 權值股（台積電、聯發科、鴻海那些）。

**請問觀察池到底要用哪些？**
- A: 0050 成分股（大型權值股，漲幅通常小，1~3% 居多）
- B: 全市場中小型股（漲幅大、比較像「飆股明牌」）
- C: 你有特定清單？

回答：


## 4. 股票資訊注入 LLM Prompt 的時機

SDD Step 4 說要把明牌股票放進系統提示，讓阿凱老師自然提到。但目前的架構：
- `llm_client.py` 的 system prompt 是靜態的，沒有動態注入股票資訊的機制
- 階段判斷完全靠 LLM 自己從對話輪數推斷，程式端沒有追蹤

**你希望怎麼做？**
- A: 每次呼叫 `generate_reply()` 都抓一次股票資訊，附加到 system prompt 尾部（簡單直接）
- B: 只在第二階段（展示實力）以後才注入（需要程式端判斷階段）
- C: 其他方式？

回答：


## 5. `backend/tools/` 目錄結構

SDD 指定新建 `backend/tools/market_data.py`，但目前 `backend/` 下沒有 `tools/` 資料夾，所有模組都是平放的（`database.py`, `llm_client.py`, `image_generator.py`）。

**要按照 SDD 新建 `backend/tools/` 目錄，還是直接在 `backend/` 下平放 `market_data.py`？**

回答：


## 6. FAL_KEY 的處理

目前 `image_generator.py` 裡 FAL_KEY 是硬編碼在程式裡的，而且 `docker-compose.yml` 的 `fastapi_backend` 環境變數也沒有傳 `FAL_KEY`。

- 如果最終改用 Pillow 出圖，Fal.ai 就可以整個拿掉
- 如果還需要 Fal.ai，FAL_KEY 應該移到環境變數 / docker-compose

**這部分怎麼處理？**

回答：


## 7. 出圖是否需要 session 隔離

目前 `generate_profit_image()` 輸出固定寫到 `assets/generated-picture.png`，如果有多個 session 同時操作會互相覆蓋。

**目前 MVP 階段是否只有單一 session，還是需要支援多 session 並行？**

回答：
